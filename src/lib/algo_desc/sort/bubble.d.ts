import makeAlgoDesc from "../makeAlgoDesc";

export function makeBubbleAlgoDesc(arr): ReturnType<typeof makeAlgoDesc>;

const exampleArr: number[];

const bubbleAlgoDescExample: ReturnType<typeof makeAlgoDesc>;

export default [bubbleAlgoDescExample, exampleArr];
